---
completed_at: '2026-04-05T00:00:00'
created: '2026-04-05'
depends_on: []
id: AGE-020
parent: AGE-017
priority: medium
status: done
title: Add --reason flag to hv task update for blocked tasks
type: task
updated: '2026-04-05'
---

## Description
Add a `--reason` option to `hv task update` that stores a blocking reason in the task frontmatter as `blocked_reason`. Used by the orchestrator when marking tasks blocked after retry exhaustion.

## Spec References
- `projects/agent-hivemind/features/08_search-and-feedback-improvements.md` — section 3
- `src/hivemind/commands/task.py` — `update` command

## Completion Criteria
- [ ] `hv task update AGE-XXX --status blocked --reason "reason text"` stores `blocked_reason` in frontmatter
- [ ] `hv task get AGE-XXX` displays the blocked_reason
- [ ] `--reason` without `--status blocked` is accepted (stores reason regardless)
- [ ] `hv task get --format json` includes blocked_reason in output
- [ ] New test for --reason flag
- [ ] `ruff check src/ tests/` passes
- [ ] `mypy src/` passes
- [ ] `pytest` passes