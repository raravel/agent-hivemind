---
completed_at: '2026-05-22T00:10:45.281703'
created: '2026-05-21'
depends_on:
- AGE-005-07ca
id: AGE-006-d923
parent: AGE-002-141a
priority: medium
status: done
title: '/hv:plan SKILL: inline scope + references/scope-backfill.md'
type: task
updated: '2026-05-22'
---

## Description
Update `/hv:plan` SKILLs (claude and codex variants) so Phase 2 sets scope on every new leaf task, and add an opt-in "back-fill scope on existing tasks" mode whose full procedure lives in `references/scope-backfill.md`. The main SKILL.md gains ~5 lines inline + ~4 lines pointer — new projects pay almost no context cost. Also adapt SKILL to the new `hv run` JSON object output and document the `scope` field in `references/task-format.md`.

## Spec References
- [[features/10_scope-aware-parallel|10_scope-aware-parallel]] `../docs/features/10_scope-aware-parallel.md` — §Plan-time obligations, §CLI surface
- [[rules]] `../docs/rules.md` — speculative scope NEVER rule
- [[verify]] `../docs/verify.md`

## Declared Scope (informational until T2 lands the CLI)
- `src/hivemind/plugin/skills/claude/plan/SKILL.md`
- `src/hivemind/plugin/skills/claude/plan/references/scope-backfill.md`
- `src/hivemind/plugin/skills/claude/plan/references/task-format.md`
- `src/hivemind/plugin/skills/codex/hv-plan/SKILL.md`
- `src/hivemind/plugin/skills/codex/hv-plan/references/scope-backfill.md`
- `src/hivemind/plugin/skills/codex/hv-plan/references/task-format.md`

## Changes to SKILL.md (claude + codex mirrors)
- Phase 2 task creation: add ~5 lines instructing the planner to set `--scope` on every leaf task; use `["*"]` honestly when the write set is unenumerable
- Add example `hv task create ... --scope src/foo.py --scope manifest:python` to the existing example block
- Mode decision tree: add Mode (c) Scope back-fill — ~4 lines with a pointer to `references/scope-backfill.md`
- Important Rules: add "ALWAYS set scope on new leaf tasks" / "NEVER write speculative paths into scope"
- Update example using `hv run --ready-only --limit N` to expect the new `{tasks, deferred}` object output

## Changes to references/scope-backfill.md (new file)
- Procedure for back-filling scope on existing scope-less pending tasks
- Steps: list scope-less pending tasks → for each, read `## Spec References` → read referenced feature's `## Implementation` → propose minimum honest subset → show per-task diff to user → single yes/no confirm → apply via `hv task scope-set`
- Stress: never auto-apply, never speculate; if the minimum honest subset cannot be identified, propose `["*"]`
- Reports the count of `["*"]` proposals separately so the user can spot under-specified features

## Changes to references/task-format.md
- Add `scope` row to the frontmatter schema table (optional, list[str], format described in feature 10)
- Add short note on conflict semantics and scope as future reservation

## Completion Criteria
- [ ] `claude/plan/SKILL.md` and `codex/hv-plan/SKILL.md` mention `--scope` in their Phase 2 example block
- [ ] Both SKILLs reference `Mode (c) Scope back-fill` with a pointer to `references/scope-backfill.md`
- [ ] Inline scope obligation in main SKILL.md is no more than ~5 lines per file (context budget discipline)
- [ ] `claude/plan/references/scope-backfill.md` and `codex/hv-plan/references/scope-backfill.md` exist with the procedure body
- [ ] Both `task-format.md` files document the `scope` field with one row in the schema table
- [ ] SKILL.md text describing `hv run --ready-only --format json` matches the actual object output shape from T3
- [ ] All commands in `../docs/verify.md` pass